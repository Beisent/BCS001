## 颜色缓冲

### 为什么我们需要颜色缓冲？

目前，我们直接将颜色数据顺序写入 PPM 文件。这种方式虽然简单，但存在显著的局限性：

- **不可随机访问：** 你无法直接修改已经写入文件的像素点。
- **缺乏后处理能力：** 无法进行抗锯齿、模糊、色调映射等需要全局或邻域像素数据的操作。
- **性能瓶颈：** 频繁的磁盘 I/O（输入/输出）远比内存读写缓慢。

通过引入**颜色缓冲（Color Buffer）**，我们将渲染逻辑与文件输出解耦：先在高速内存中完成所有的计算与修改，待整帧画面渲染完成后，再将其一次性持久化到 PPM 文件中。

## 具体实现

请看s001